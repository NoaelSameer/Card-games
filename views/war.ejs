<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>War Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-[#a3b18a] flex flex-col items-center justify-center h-[100vh]">
    <h1 class="text-white text-[4vw] text-center">War</h1>
    <h3 class="text-white text-center text-[1.25vw] mt-4 max-w-xl"><%= info %></h3>
    <div class="flex justify-between w-full max-w-[40vw] mt-[2vh]">
        <div class="text-center w-1/2">
            <p class="text-white text-[1.25vw] mb-4">Player Card:</p>
            <img src="<%= pCard.data.cards[0].image %>" alt="player card" class="mx-auto w-[10vw] h-auto">
            <p class="text-white text-[1.25vw] mt-4">Player Score:</p>
            <p class="text-white text-[2vw]"><%= pTotal %></p>
        </div>

        <div class="text-center w-1/2">
            <p class="text-white text-[1.25vw] mb-4">Bot Card:</p>
            <img src="<%= bCard.data.cards[0].image %>" alt="bot card" class="mx-auto w-[10vw] h-auto">
            <p class="text-white text-[1.25vw] mt-4">Bot Score:</p>
            <p class="text-white text-[2vw]"><%= bTotal %></p>
        </div>
    </div>

    <form action="/war" class="mt-8">
        <button name="action" value="compare" type="submit" class="px-6 py-3 bg-[#588157] text-white rounded-lg hover:bg-[#3a5a40] transition duration-100">
            Compare
        </button>
    </form>

    <div id="winner" class="mt-8 text-white text-2xl">
        The Winner is: 
        <p class="font-bold"><%= winner %></p>
    </div>

    <form action="/reset" class="mt-4">
        <button type="submit" class="px-6 py-3 bg-[#588157] text-white rounded-lg hover:bg-[#3a5a40] transition duration-100">
            Reset
        </button>
    </form>
</body>
</html>